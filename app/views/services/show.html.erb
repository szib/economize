<% content_for :card_bottom do %>
  <div class="ui bottom attached button">
    <i class="edit icon"></i>
    <%= link_to 'Edit service', edit_service_path(@service) %>
  </div>
<% end %>
<div class="ui container">
  <div class="ui one column cards">
    <%= render 'card', service: @service %>
  </div>
</div>
